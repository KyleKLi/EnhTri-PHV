# GOA resource

Place `goa_human.gaf` or `goa_human.gaf.gz` in this directory. Step 2 uses the
file to map GO identifiers to the biological process (BP), molecular function
(MF), and cellular component (CC) namespaces.

For a frozen reproduction, record the source URL, download date, release
metadata, and SHA256 hash of the exact file used.
